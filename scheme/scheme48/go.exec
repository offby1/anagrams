;; -*- scheme -*-
,config ,load packages.config
,user
,open anagrams
,time (let ((input-string "Ernest Hemingway"))
        (display (length (anagrams input-string)))
        (display " anagrams of ")
        (display input-string)
        (newline))
,exec
(exit)
